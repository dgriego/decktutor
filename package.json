{
  "name": "mtgline",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check deck-core.js && node --check hand-engine.js && node --check lib/deck-service.js"
  },
  "dependencies": {
    "@vercel/functions": "3.9.6"
  }
}
